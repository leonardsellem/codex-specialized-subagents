# Copy to .env and fill in secrets. This file is safe to commit.

# --- OpenAI API (used by optional tooling like AI commit messages) ---
# Prefer storing keys in your shell environment if you can.
OPENAI_API_KEY=

# Optional: if you prefer naming it this way
# CODEX_API_KEY=

# Optional: override API base (rare; e.g. proxies)
# OPENAI_BASE_URL=https://api.openai.com/v1

# --- AI commit message hook (optional) ---
# Set to 0 to disable the hook without uninstalling it.
AI_COMMIT_ENABLED=1

# Model for generating commit messages
AI_COMMIT_MODEL=gpt-5.2

# Reasoning effort: none|low|medium|high|xhigh (gpt-5.2 supports none as default)
AI_COMMIT_REASONING_EFFORT=none

# Output verbosity: low|medium|high
AI_COMMIT_VERBOSITY=low

# Truncation guardrails (avoid sending huge diffs)
AI_COMMIT_MAX_DIFF_CHARS=20000

# If 1, a hook failure aborts commits. Default is non-blocking.
AI_COMMIT_STRICT=0
