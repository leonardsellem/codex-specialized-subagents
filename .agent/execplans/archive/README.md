# ExecPlans archive

Completed ExecPlans live here.

Keep the plan documents (the `.md` files). By default, artifacts are gitignored — you can selectively commit small, non-sensitive artifacts if they add value.
